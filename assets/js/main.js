
const $=(s,c=document)=>c.querySelector(s), $$=(s,c=document)=>[...c.querySelectorAll(s)];
const navBtn=$('.nav-toggle'), nav=$('.nav-links'); if(navBtn){navBtn.addEventListener('click',()=>nav.classList.toggle('open'))}
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12}); $$('.reveal').forEach(el=>io.observe(el));
$$('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{ $$('[data-filter]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');let f=btn.dataset.filter;$$('[data-category]').forEach(c=>{c.style.display=(f==='All'||c.dataset.category===f)?'block':'none'}) }));
const compare=$('.compare'); if(compare){let range=$('input[type=range]',compare),after=$('.after',compare),divider=$('.divider',compare); range.addEventListener('input',()=>{let v=range.value;after.style.clipPath=`inset(0 ${100-v}% 0 0)`;divider.style.left=v+'%'})}
$$('.clickable').forEach(img=>img.addEventListener('click',()=>{let l=$('.lightbox');$('img',l).src=img.src;l.classList.add('open')})); const lb=$('.lightbox'); if(lb){$('button',lb).onclick=()=>lb.classList.remove('open');lb.addEventListener('click',e=>{if(e.target===lb)lb.classList.remove('open')})}
const f=$('#contact-form'); if(f)f.addEventListener('submit',e=>{e.preventDefault();$('.notice').style.display='block';f.reset()});
const est=$('#estimator'); if(est){const calc=()=>{let type=$('#etype').value,size=+$('#esize').value,level=$('#elevel').value;base={Residential:145,Commercial:165,Hospitality:190,Minimalist:155}[type],mult={Essential:1,Signature:1.35,Bespoke:1.8}[level],design=Math.round(size*base*mult),ff=Math.round(size*(level==='Bespoke'?520:level==='Signature'?360:230)),cont=Math.round((design+ff)*.08),total=design+ff+cont;$('#size-out').textContent=size+' m²';$('#design-fee').textContent='€'+design.toLocaleString();$('#ff-cost').textContent='€'+ff.toLocaleString();$('#contingency').textContent='€'+cont.toLocaleString();$('#estimate-total').textContent='€'+total.toLocaleString()+'–€'+Math.round(total*1.18).toLocaleString()};['etype','esize','elevel'].forEach(id=>$('#'+id).addEventListener('input',calc));calc()}
$$('[data-count]').forEach(el=>{let target=+el.dataset.count;let done=false;let obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting&&!done){done=true;let n=0,step=Math.max(1,Math.ceil(target/40));let t=setInterval(()=>{n=Math.min(target,n+step);el.textContent=n+(target>20?'+':'');if(n>=target)clearInterval(t)},30)}}));obs.observe(el)});
